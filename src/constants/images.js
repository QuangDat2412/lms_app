/* eslint-disable global-require */

export const IMAGE_FILE = {
    noImages: require('asset/images/no_image.png'),
    // noImages: `${link_asset}/${image_dir}/no_image.png`,
};

/* eslint-enable global-require */
